var display, camera

export default class Handler {
    static setDisplay(display) {
        display = display
    }

    static getDisplay() {
        return display
    }

    static setCamera(camera) {
        camera = camera
    }

    static getCamera() {
        return camera
    }
}