export default class Sprite
{
    constructor (buffer, x, y, width, height)
    {
        this.buffer = buffer
        this.x = x
        this.y = y
        this.width = width
        this.height = height
    }
}